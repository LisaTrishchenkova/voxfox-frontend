name: Deploy to Staging (Voxfox)

on:
  push:
    branches: ["staging"]
    paths:
      - "src/**"
      - "public/**"
      - "package.json"
      - "vite.config.ts"
  workflow_dispatch: # ручной запуск

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # - name: Lint check
      #   run: npm run lint || true

      # - name: Type check
      #   run: npm run type-check || true

      - name: Build project
        run: npm run build:staging
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_ENVIRONMENT: staging

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Deploy to S3
        run: |
          aws s3 sync ./dist/ s3://voxfox-frontend-dev-703671934401/ \
            --delete \
            --cache-control "max-age=31536000,public" \
            --exclude "index.html"

          # index.html без кэширования
          aws s3 cp ./dist/index.html s3://voxfox-frontend-dev-703671934401/index.html \
            --cache-control "no-cache, no-store, must-revalidate"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/index.html" "/assets/*"

      # - name: Send Telegram notification
      #   if: always()
      #   uses: appleboy/telegram-action@master
      #   with:
      #     to: ${{ secrets.TELEGRAM_CHAT_ID }}
      #     token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      #     message: |
      #       🚀 Деплой Voxfox ${{ github.ref_name }}
      #       Статус: ${{ job.status }}
      #       Коммит: ${{ github.sha }}
      #       Автор: ${{ github.actor }}
      #       Ссылка: https://voxfox.dev.bafid.app
