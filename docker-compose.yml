networks:
  voxfox-network:
    driver: bridge

services:
  voxfox:
    container_name: voxfox
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    networks:
      - voxfox-network
