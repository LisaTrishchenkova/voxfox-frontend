networks:
  voxfox-network:
    driver: bridge

services:
  voxfox:
    container_name: voxfox
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5001:80"
    networks:
      - voxfox-network
